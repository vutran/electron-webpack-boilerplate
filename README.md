# Electron/React Boilerplate

A quick-start boilerplate loaded with features:

- webpack
- React
- Hot Loader
- Babel/ES2015
- CSS Modules
- Sass, PostCSS, Autoprefixer
- dotenv

## Run with npm

npm install && npm start
